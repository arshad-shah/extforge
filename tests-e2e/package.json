{
  "name": "@extforge/tests-e2e",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "packageManager": "pnpm@10.0.0",
  "description": "Browser-based e2e tests. Builds the example extensions, loads them into a real Chromium instance via Playwright's persistent context, and validates popup, content script, background SW, and HMR end-to-end.",
  "scripts": {
    "test": "playwright test",
    "test:headed": "playwright test --headed",
    "build:fixtures": "pnpm -r --filter \"@extforge-example/*\" build:chrome",
    "install-browsers": "playwright install --with-deps chromium"
  },
  "devDependencies": {
    "@playwright/test": "^1.48.0",
    "@types/chrome": "^0.0.290",
    "@types/node": "^25.9.3",
    "@types/ws": "^8.5.13",
    "ws": "^8.18.0"
  }
}

---
title: modules
description: "List of ExtForge modules — a superset of plugins that can also add entrypoints, extend the manifest, and contribute types/runtime code. Accepts module objects, package names, or local paths. Runs after built-in presets and before `plugins`. See `extforge/modules`."
---

List of ExtForge modules — a superset of plugins that can also add entrypoints, extend the manifest, and contribute types/runtime code. Accepts module objects, package names, or local paths. Runs after built-in presets and before `plugins`. See `extforge/modules`.

**Type:** `Array<unknown>`


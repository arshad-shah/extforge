---
title: css
description: "CSS strategy: a built-in preset (`'tailwind'`, `'vanilla'`, `'none'`) or a custom processor object (`{ name, transform }` or `{ name, command, args }`). See the [Styling guide](/guides/styling/)."
---

CSS strategy: a built-in preset (`'tailwind'`, `'vanilla'`, `'none'`) or a custom processor object (`{ name, transform }` or `{ name, command, args }`). See the [Styling guide](/guides/styling/).

**Type:** `'tailwind' \| 'vanilla' \| 'none' \| object`

**Default:** `"tailwind"`


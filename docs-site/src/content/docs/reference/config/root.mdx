---
title: root
description: Override the project root. Defaults to the directory `extforge` is invoked from.
---

Override the project root. Defaults to the directory `extforge` is invoked from.

**Type:** `string`


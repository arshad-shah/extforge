---
title: framework
description: UI framework. Drives auto-injection of first-party plugins (e.g., `presetReact()` when set to `react`).
---

UI framework. Drives auto-injection of first-party plugins (e.g., `presetReact()` when set to `react`).

**Type:** `'react' \| 'vanilla'`

**Default:** `"react"`


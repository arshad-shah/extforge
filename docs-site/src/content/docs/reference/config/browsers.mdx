---
title: browsers
description: Browsers to build for. ExtForge generates a per-browser manifest into `dist/<browser>/`.
---

Browsers to build for. ExtForge generates a per-browser manifest into `dist/<browser>/`.

**Type:** `Array<'chrome' | 'firefox' | 'edge' | 'safari'>`

**Default:** `["chrome", "firefox"]`

